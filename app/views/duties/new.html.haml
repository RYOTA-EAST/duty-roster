%div.duty-form
  %h1 新規当番表
  = form_with model: @duty, local: true do |f|
    = f.label :当番表名, class: 'chat-room-form__label'
    = f.text_field :name, class: 'chat__room_name chat-room-form__input', placeholder: '当番表名を入力してください'
    %select{:name => "duty[user_ids][]"}
      %option{:value => ""} 当番表に参加するユーザーを選択
      - User.where.not(id: current_user.id).each do |user|
        %option{:value => user.id }= user.nickname
    %input{:name => "duty[user_ids][]", :type => "hidden", :value => current_user.id}

    = f.submit